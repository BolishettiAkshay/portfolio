# Portfolio

Simple static portfolio. Open `index.html` in a browser to preview.

How to use

- Edit the top of `index.html` to add your name, bio, projects, and email.
- Replace placeholder links with real project URLs or GitHub repos.

Deploy

- Push this folder to a GitHub repository and enable GitHub Pages from the repository settings, or use any static host (Netlify, Vercel, Surge).
